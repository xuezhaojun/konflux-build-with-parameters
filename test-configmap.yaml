apiVersion: v1
kind: ConfigMap
metadata:
  name: build-parameters
  namespace: zxue-tenant
data:
  CI_VERSION_2_10: "2.10.5"
  CI_VERSION_2_9: "2.9.8"
  # Add more parameters used in build
  # APP_NAME: "my-application"
  # BUILD_ENV: "production"
